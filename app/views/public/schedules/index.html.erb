<div class="row">
  <%= render 'shared/sidebar' %>
  <div class="col-sm-9">
    <div class="title">
      <h2><i class="far fa-calendar-alt icon-color-blue"></i>スケジュール一覧</h2>
    </div>
    <div id="scroll">
      <%= render 'schedule_posts', schedules: @schedules, cuurent_employee: current_employee %>
      <%= paginate @schedules, :theme=>'scroll' %>
    </div>
  </div>
</div>